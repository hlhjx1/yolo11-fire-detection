# 项目说明

## 项目概述

本项目基于 Ultralytics YOLO11 代码库，面向论文 **A New Collaborative Detection Method for Forest Fires Under Degraded Image Conditions** 的实验实现与结果整理。

论文提出了 **CoDeF-Net**（Collaborative Detection Framework Network）框架，用于在雾霾、低照度、对比度不足等退化图像条件下提升森林火灾中烟雾和火焰的检测能力。项目的核心思路是把图像增强和目标检测结合起来，而不是把增强当成独立的前处理步骤。

## 论文核心方法

论文的主要贡献可以概括为三部分：

1. **Retinex-BCE 图像增强**
   使用基于 Retinex 的亮度分解思想，对退化图像进行自适应增强，尽量恢复烟雾和火焰的可见性，同时避免过度平滑。

2. **Bright Channel Enhancement（BCE）**
   针对森林火灾场景中“高亮火焰”这一特殊现象，引入亮通道先验来修正传统暗通道方法在火灾图像上的不适用问题，从而更好地保留火焰边缘和浓烟结构。

3. **与 YOLOv11 协同检测**
   将增强后的图像输入 YOLOv11 进行检测，使模型在复杂环境下对小火焰、稀薄烟雾和远距离火点具有更好的鲁棒性。

## 数据集说明

论文中构建了一个包含 **1757 张真实森林火灾图像** 的数据集，覆盖了多种场景：

- 地面火
- 树冠火
- 低照度场景
- 雾霾或烟雾遮挡场景
- 不同尺度和背景下的火灾图像

数据集按照 **7:1:2** 划分为训练集、验证集和测试集。

## 论文结果

论文报告的主要结果包括：

- Retinex-BCE 的 FSIMC 达到 **0.9611**
- Retinex-BCE 的 LOE 为 **254.78**
- CoDeF-Net 在 AP@0.5 上达到 **87.9%**
- 相比原始 YOLOv11，检测性能提升约 **3.8%**

这些结果说明该方法对退化图像下的森林火灾检测有较好的实际应用价值。

## 仓库内容与用途

本仓库主要用于：

- 森林火灾目标检测训练与推理
- 退化图像增强与检测协同实验
- 结果曲线绘制与对比分析
- 论文相关实验复现与展示

常见入口文件包括：

- `train.py`：训练模型
- `val.py`：验证模型
- `detect.py`：推理检测
- `export.py`：模型导出
- `plot_result*.py`：结果曲线绘制

## 快速使用

### 1. 安装环境

建议使用 Python 3.11 及 PyTorch 对应环境，并安装项目依赖。

### 2. 训练

```bash
python train.py
```

### 3. 验证

```bash
python val.py
```

### 4. 推理

```bash
python detect.py
```

如果你使用的是自定义权重，请在 `detect.py` 中把 `best.pt` 路径改成你的模型权重路径。

## 结果展示

仓库中的 `plot_result.py`、`plot_result_detect.py` 和 `plot_result_enh.py` 用于绘制训练结果和不同增强方案的对比曲线。若你已经生成了 `runs/train/fire/...` 下的训练记录，这些脚本可以直接用于生成论文图表。

## 上传到 GitHub 的建议

如果你要把这个项目传到自己的 GitHub 仓库，可以按下面的顺序操作：

1. 在 GitHub 新建一个空仓库。
2. 在本地项目目录执行 `git init`。
3. 把当前代码加入版本控制：`git add .`
4. 提交一次初始版本：`git commit -m "init: add fire detection project"`
5. 绑定远程仓库：`git remote add origin <你的仓库地址>`
6. 推送到远程分支：`git push -u origin main`

如果你本地仓库已经存在，只需要执行第 3 步到第 6 步。

## 论文引用

如果你在仓库中引用这篇论文，建议保留原始信息：

**A New Collaborative Detection Method for Forest Fires Under Degraded Image Conditions**

Remote Sensing, 2026, 18(12), 1880.
DOI: 10.3390/rs18121880

## 说明

本说明文件根据论文内容整理，适合作为仓库首页说明、项目介绍或提交 GitHub 时的补充文档。
